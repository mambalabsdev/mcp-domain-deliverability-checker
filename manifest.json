{
  "manifest_version": "0.3",
  "name": "mcp-domain-deliverability-checker",
  "version": "1.0.0",
  "description": "MCP server that checks email domain deliverability: SPF, DKIM, DMARC, MX, blacklist (DNSBL), domain age, and a 0-100 composite score.",
  "author": {
    "name": "Mamba Labs"
  },
  "license": "MIT",
  "server": {
    "type": "node",
    "entry_point": "build/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/build/index.js"],
      "env": {
        "APIFY_TOKEN": "${user_config.apify_token}"
      }
    }
  },
  "user_config": {
    "apify_token": {
      "type": "string",
      "title": "Apify API Token",
      "description": "Apify API token for running the actor",
      "sensitive": true,
      "required": false
    }
  }
}
